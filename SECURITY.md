# 安全策略

## 报告漏洞

**请不要用公开 issue 报告安全漏洞。**

请通过 [GitHub Security Advisories](https://github.com/279814/relay-gate/security/advisories/new)
私下报告。这是一个个人维护的项目，我会尽力在合理时间内回应，但无法承诺 SLA。

报告时请尽量包含：影响范围、复现步骤、以及你认为的严重程度。

## 这个项目的威胁模型

relay-gate 的设计前提是**单人或小团队自用**，不是多租户 SaaS。理解这一点对判断
「什么算漏洞」很重要。

### 在威胁模型内

- **relay key 泄露** —— 泄露等于把你所有上游 key 的用量免费送人。
  这是本项目最核心的凭据。
- **管理面未授权访问** —— 管理 API 能读写所有上游配置。它与转发端点的
  泄露后果差一个量级，因此公网模式下额外加 IP 白名单。
- **上游 key 泄露** —— 数据库中 AES-GCM 加密存储；样本与日志里脱敏。
- **样本库泄露** —— 样本含**明文的**完整对话原文（加密只保护上游 key）。
  库文件权限因此收到 0600。
- **A 站的 key 被发给 B 站** —— 请求内重试换站时，上一次尝试的鉴权头必须
  完全重建。有专门的端到端测试从「站那一侧」验证这件事。

### 不在威胁模型内

- **容器逃逸** —— 非 root 运行主要是为了库文件权限与宿主目录属主，
  不是把容器当安全边界。
- **同机其它用户** —— 0600 挡的是同机普通用户的顺手读取，不防本机 root。
- **上游站本身的可信度** —— 你的对话内容会发给你自己配置的上游站。
  这个项目不改变、也无法改变那件事。
- **拒绝服务** —— 单副本设计，没有速率限制。它假设客户端是你自己。

## 部署时最容易踩的三个坑

这三条都会**静默**地把服务暴露出去，而不是报错：

1. **端口绑定写成 `18787:18787`。** Docker 会插一条 iptables 规则把它直接暴露到
   公网，**且绕过 ufw/firewalld** —— `ufw status` 显示一切正常。
   `compose.yaml` 里刻意写成 `127.0.0.1:${RELAY_PORT}:18787`，
   并有 `scripts/check-deploy.sh` 守着这一行。

2. **`RELAY_ALLOW_IPS` 忘了配。** Caddyfile 里的判据是 `not remote_ip {$RELAY_ALLOW_IPS}` ——
   空列表匹配任何来源，于是管理面**全部 403**。这个方向是刻意的：
   忘了配的后果应该是「我进不去」（立刻发现），而不是「全世界都能进」（永远不会发现）。

3. **`.env` 带着 CRLF 从 Windows 拷到 Linux。** `ENCRYPTION_KEY` 的值会多一个 `\r`，
   在 Windows 上照常加解密，转到 Linux 后 `\r` 消失、密钥实际改变，
   **所有已存的上游 key 全部无法解密**。`.gitattributes` 已把 `.env.example` 锁成 LF。

## 密钥管理

`ENCRYPTION_KEY` **丢失后已存的上游 key 无法恢复**，只能一个个重填。
它不是可以随手轮换的东西——换这个值等同于清空所有上游 key。请单独备份。
