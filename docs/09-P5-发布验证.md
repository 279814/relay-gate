# P5 v1.0.0 发布验证实施记录

- 基线：`docs/01-需求与设计.md` §20.6
- 状态：离线发布门禁与发布说明已与 main 事实对齐；旧域名部署文档（docs/03）与 Caddy/nginx 脚本已删除；支持安装为 HTTP IP:port。

## 已交付

1. `internal/release` 跨包门禁测试：空库→schema 6、Keyring、备份拒绝门、安全扫描不改字节、Transform 未绑定透传、RecoveryGate single-flight。
2. `scripts/check-p5.sh`：跑 release 测试、断言发布说明仍在、禁止跟踪 `.env` / `upstreams.tsv` / `docs/02`。
3. CI 增加 P5 offline gate 步骤。
4. `docs/RELEASE-NOTES-v1.0.0.md`：按已合并 PR 诚实列出已交付与 Deferred。
5. 旧 sample 行信封加密迁移：启动时 `MigrateSampleEnvelopes`；`GetSample` 仍 dual-read 未迁移行。
6. 首次凭据 bootstrap journal（§12.3）：见 docs/06；长期进程首次启动可自动生成并打印一次。
7. 旧环境变量 migration journal（§12.8）、CLI `reset-admin`（§12.5）、登录/Bearer 读 Argon2id：见 docs/06。
8. ENCRYPTION_KEY / RELAY_KEYS 从 bootstrap/migrate 产物启动（env 优先）。
9. IP 证书续期可观测（§23）：`acmeip.RenewWatch` 本地 fake；**不**冒充真实公网续期。
10. HTTP IP:port 一行部署：删除 docs/03 / Caddyfile / deploy-nginx.sh；compose 发布所有接口；无登录 IP 白名单。

## Deferred

- 需求 §12.2 公网 IP HTTPS / Certbot **真实证书签发**（非当前安装路径）
- 长流压测与多日生产验证（本地/CI 时长与上游依赖不允许伪称完成）
- 确认后的旧 sample 明文 dual-read 清理

下一步：日常安装以 README 为准；证书类需求仍属未关闭实验项。
