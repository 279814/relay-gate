# P5 v1.0.0 发布验证实施记录

- 基线：`docs/01-需求与设计.md` §20.6
- 状态：本分支交付离线发布门禁与发布说明；**不**删除 `docs/03`

## 已交付

1. `internal/release` 跨包门禁测试：空库→schema 3、Keyring 重启加载、备份缺授权拒绝、安全扫描不改字节、Transform 未绑定透传、RecoveryGate single-flight。
2. `scripts/check-p5.sh`：跑 release 测试、断言 docs/03 与发布说明仍在、禁止跟踪 `.env` / `upstreams.tsv` / `docs/02`。
3. CI 增加 P5 offline gate 步骤。
4. `docs/RELEASE-NOTES-v1.0.0.md`：诚实列出各阶段已交付与 Deferred。

## Deferred

- 公网 IP HTTPS / Certbot 状态机与容器公网 smoke（docs/03 继续有效）
- 旧 sample 行信封加密迁移与确认后的旧格式清理
- 长流压测与多日 soak（本地/CI 时长与上游依赖不允许在本 PR 伪称完成）
- P1–P4 Deferred 项的收口（见 docs/05–08）

下一步：whole-repo 最终审查 PR（若本门禁合并后仍有真实缺陷再开）。
