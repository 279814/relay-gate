# P5 v1.0.0 发布验证实施记录

- 基线：`docs/01-需求与设计.md` §20.6
- 状态：离线发布门禁与发布说明已与 main 事实对齐；**不**删除 `docs/03`；**不**伪称多日 soak 或公网证书

## 已交付

1. `internal/release` 跨包门禁测试：空库→schema 6（含 `security_finding` / `transform_set`）、Keyring 重启加载、备份缺授权拒绝、安全扫描不改字节、Transform 未绑定透传、RecoveryGate single-flight。
2. `scripts/check-p5.sh`：跑 release 测试、断言 docs/03 与发布说明仍在、禁止跟踪 `.env` / `upstreams.tsv` / `docs/02`。
3. CI 增加 P5 offline gate 步骤。
4. `docs/RELEASE-NOTES-v1.0.0.md`：按已合并 PR 诚实列出已交付与 Deferred。
5. 旧 sample 行信封加密迁移：启动时 `MigrateSampleEnvelopes` 就地改写明文 BLOB 为 `v1:` 信封；`GetSample` 仍 dual-read 未迁移行；不删除历史。
6. 首次凭据 bootstrap journal（§12.3）：见 docs/06；本地可测崩溃恢复与 Argon2id 落盘。
7. 旧环境变量 migration journal（§12.8）、CLI `reset-admin`（§12.5）、登录/Bearer 读 Argon2id（仍兼容 env 明文）：见 docs/06。
8. 无环境变量时 ENCRYPTION_KEY / RELAY_KEYS 从 bootstrap/migrate 产物启动（env 优先；见 docs/06 §已交付 12；`internal/config` 单测）。

## Deferred

- 公网 IP HTTPS / Certbot **真实公网证书签发与容器 smoke**（docs/03 继续有效；本地状态机见 docs/06）
- 长流压测与多日 soak（本地/CI 时长与上游依赖不允许伪称完成）
- 确认后的旧 sample 明文 dual-read 清理（迁移已交付；去掉明文回退需运维确认全库已信封化）
- README 替换 docs/03 后的删除清单（Caddyfile、deploy-nginx.sh；**真实公网证书验证前不得删**）

下一步：硬门槛仍含「真实公网证书 + 容器 smoke」与「多日生产 soak」。
