# P3 内容安全实施记录

- 基线：`docs/01` §14、§13.6、§20.4
- 状态：本分支交付被动扫描核心与 Security Center API

## 已交付

1. `internal/security`：被动文本规则（script / javascript: / on* / 常见提示注入），旁路不改响应。
2. `security.Center`：有界 finding 环；纯文本 detail。
3. API：`GET /admin/api/security/findings`、`POST /admin/api/security/scan`、`POST /admin/api/security/canary`（Lazy 拒绝；本 PR 不发上游 canary 请求）。
4. main 装配 Security Center。

## Deferred

- SMTP 发送与无敏感正文模板
- 主动 canary 上游调用与周期 canary（需 Active）
- Security Center 管理 UI 页
- 告警聚合/审计持久化表
- 旁路接入真实流量 Observation（当前为管理诊断扫描入口）

下一步：P4 声明式转换。
