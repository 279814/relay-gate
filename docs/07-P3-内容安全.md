# P3 内容安全实施记录

- 基线：`docs/01` §14、§13.6、§20.4
- 状态：本分支关闭 docs/07 既有 Deferred（周期 canary 保持可选默认关闭）

## 已交付

1. `internal/security`：被动文本规则（script / javascript: / on* / 常见提示注入），旁路不改响应。
2. `security.Center` / `PersistentCenter`：有界 finding 环 + SQLite `security_finding` 持久化（schema 5）。
3. **真实流量旁路**：`proxy.Handler` 经 `security.Observer` 有界队列扫描响应副本；样本开关关闭时仍扫描；队列满丢弃不阻塞转发。
4. API：`GET /admin/api/security/findings`、`POST /admin/api/security/scan`、`POST /admin/api/security/canary`（Lazy 拒绝；Active 走 `probeAdmin.RunManual`）、`GET/PUT /admin/api/security/smtp`、`POST .../smtp/test`。
5. SMTP：`AlertMailer` 元数据模板（无对话正文）；本地 fake SMTP 单测；密码信封加密。
6. Security Center UI：`security.mjs`（不改脏 `app.js`）。

## Deferred

（无。可选周期 canary 默认关闭，非 v1.0.0 硬门槛；告警限速为进程内窗口，finding 已落库。）

下一步：P4 声明式转换。
