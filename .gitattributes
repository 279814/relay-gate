# 默认按文本处理，行尾交给各平台
* text=auto

# 内置探活模板（P0-06）。这些是 embed 进二进制的数据文件，而 body_text 的
# 内容会**逐字节**成为发出去的请求体。行尾按平台变化的话，同一份模板在
# Windows 检出与 Linux 检出上会产生不同的字节 —— 而 `* text=auto` 恰恰
# 允许那种变化。
internal/probe/builtin/**/*.json text eol=lf
internal/probe/builtin/*.json    text eol=lf

# 这些必须是 LF：目标运行环境是 Linux + Docker，
# CRLF 会让 shell 直接报 "bash: \r: command not found"
*.sh   text eol=lf
*.py   text eol=lf
*.tsv  text eol=lf
Dockerfile      text eol=lf
docker-compose.yml text eol=lf
*.go   text eol=lf
go.mod text eol=lf
go.sum text eol=lf

# M7 的部署清单。这几个同样必须是 LF，而 `* text=auto` 给不了这个保证 ——
# Windows 上 core.autocrlf=true 时它们会被检出成 CRLF（实测过全新克隆）。
#
# compose.yaml 的 `command:` 里嵌着一段 shell（Caddy 启动前按邮箱是否为空
# 条件删除 email 行）。YAML 块标量带 CRLF 的话，容器里的 sh 会收到行尾的
# `\r`，sed 表达式当场报错 —— 而症状是 Caddy 起不来，看不出与行尾有关。
#
# .env.example 是**后果最重的一个**：用户 `cp .env.example .env` 之后，
# compose 的 env_file 解析器不会去掉 `\r`，于是 ENCRYPTION_KEY 的值里
# 多一个回车。它照样通过长度校验、服务照常起、上游 key 也能正常加解密 ——
# 直到在 Linux 上用同一份 .env 重新部署：那时 `\r` 没了，密钥变了，
# **所有已存的上游 key 全部解不开**。（RELAY_KEYS 走 TrimSpace 不受影响，
# ENCRYPTION_KEY 与 ADMIN_PASSWORD 是裸 os.Getenv。）
compose.yaml    text eol=lf
.env.example    text eol=lf
Caddyfile       text eol=lf

# 前端资源（M5）。go:embed 本身不在意行尾，但 alpine.min.js 是**第三方
# 原样收录**的文件 —— CRLF 转换会改变它的字节与哈希，想核对「打进二进制的
# 是不是官方发布的那个」时就对不上了。同目录的其余文件一并统一，
# 免得 diff 里混着两种行尾。
*.html text eol=lf
*.css  text eol=lf
*.js   text eol=lf

# PowerShell 脚本只在 Windows 上跑，CRLF 是它的原生行尾。
# 显式写出来，免得以后误以为是漏了一条 eol=lf。
*.ps1  text eol=crlf

# SQLite 库与二进制产物不做行尾转换
*.db   binary
*.png  binary
*.ico  binary

# 协议响应夹具是逐字节 wire 输入；即使内容可读，也禁止 CRLF 归一化。
internal/probe/testdata/responses/*.bin binary
